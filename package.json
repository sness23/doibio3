{
  "name": "doibio3",
  "version": "0.1.0",
  "description": "Minimal entity-resolution graph: an append-only event log, a graph projection, and confidence-weighted identity resolution. Reference implementation of the planetar platform's entity-graph layer.",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "author": "Steven R. Ness <sness@sness.net>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sness23/doibio3.git"
  },
  "exports": "./dist/src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/*.test.js",
    "demo": "npm run build && node dist/demo/dark-vessel.js",
    "clean": "rm -rf dist data"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
